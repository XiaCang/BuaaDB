// 状态码及其含义
🔹 2xx 成功类
状态码	含义	说明
200 OK	成功	最常见，表示请求成功并返回了期望结果
201 Created	已创建	资源创建成功（常用于 POST /api/user 这类接口）
202 Accepted	已接受	请求已接受，但尚未处理完（异步处理）
204 No Content	成功但无返回内容	例如删除成功但不需要返回数据
🔹 3xx 重定向类
状态码	含义	说明
301 Moved Permanently	永久重定向	请求的资源已被永久移动到新位置
302 Found	临时重定向	临时跳转到新地址（常见于登录后跳转）
304 Not Modified	未修改	客户端缓存仍有效，可直接使用缓存内容
🔹 4xx 客户端错误类
状态码	含义	说明
400 Bad Request	错误请求	请求参数或语法错误
401 Unauthorized	未授权	需要身份验证（例如未带 Token）
403 Forbidden	禁止访问	权限不足，即使登录也不允许访问
404 Not Found	未找到资源	请求的路径或资源不存在
405 Method Not Allowed	方法不被允许	比如用 POST 调了只允许 GET 的接口
408 Request Timeout	请求超时	客户端请求超时
409 Conflict	冲突	资源冲突，如注册重复用户名
422 Unprocessable Entity	无法处理的实体	请求格式正确，但内容语义错误（常用于表单校验错误）
🔹 5xx 服务器错误类
状态码	含义	说明
500 Internal Server Error	服务器内部错误	最常见的后端异常情况
501 Not Implemented	未实现	服务器不支持当前请求的功能
502 Bad Gateway	网关错误	代理服务器收到无效响应（如 Nginx 转发）
503 Service Unavailable	服务不可用	服务器过载或维护中
504 Gateway Timeout	网关超时	代理超时未等到后端响应



/api/login

描述：用于用户登录
类型：POST

参数 
{
  "username": "用户名",
  "password": "密码（md5加密后）"
}

返回值
{
  "token": tk, // tk为生成的token值
  "message": msg  // msg为服务器返回的信息
}


/api/register

描述：用户注册
类型：POST

参数 
{
  "username": "用户名",
  "password": "密码（md5加密后）"
}

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/user

描述：获取用户信息
类型：GET

返回值
{
  "user_name": user_name, // 用户名
  "nickname": nickname, // 用户昵称
  "avatar_url": avatar_url, // 用户头像
  "phone": phone, // 用户手机号
  "intro": intro, // 用户简介
  "created_at": created_at, // 用户创建时间
  "message": msg  // msg为服务器返回的信息
}

/api/user/<id>

描述：获取用户id为<id>的信息
类型：GET

返回值
{
  "user_name": user_name, // 用户名
  "nickname": nickname, // 用户昵称
  "avatar_url": avatar_url, // 用户头像
  "phone": phone, // 用户手机号
  "intro": intro, // 用户简介
  "created_at": created_at, // 用户创建时间
  "message": msg  // msg为服务器返回的信息
}



/api/upload

描述：上传文件
类型：POST

参数
{
  "type": type, // 上传的文件类型  可以是"avatar","product"之一，分别表示头像和商品图片
  "file": file // 上传的文件
}

返回值

{
  "url": url, // 上传后的文件url
  "message": msg, // msg为服务器返回的信息
}

/api/update_user

描述：更新用户信息
类型：POST

参数
{
  "nickname": nickname, // 用户昵称
  "avatar_url": avatar_url, // 用户头像
  "phone": phone, // 用户手机号
  "intro": intro, // 用户简介
}

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/product/<id> 

描述：获取商品id为<id>的信息
类型：GET

返回值

{
  "id": id, // 商品id
  "name": name, // 商品名称
  "price": price, // 商品价格
  "image_url": image_url, // 商品图片
  "description": description, // 商品描述
  "seller_id": seller_id, // 商品卖家id
  "created_at": created_at, // 商品创建时间
  "updated_at": updated_at, // 商品更新时间
  "category_id": category_id, // 商品分类
  "status": status, // 商品状态  可以是"active","inactive","deleted","sold"之一，分别表示上架，下架，删除，已售出
  "message": msg  // msg为服务器返回的信息
}

/api/create_product

描述：创建商品
类型：POST

参数
{
  "name": name, // 商品名称
  "price": price, // 商品价格
  "image_url": image_url, // 商品图片
  "description": description, // 商品描述
  "category_id": category_id // 商品分类
}

返回值
{
  "message": msg  // msg为服务器返回的信息
}


/api/modify_product

描述：修改商品
类型：POST

参数
{
  "name": name, // 商品名称
  "price": price, // 商品价格
  "image_url": image_url, // 商品图片
  "description": description, // 商品描述
  "category_id": category_id // 商品分类
}

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/get_products

描述：获取所有商品
类型:GET

返回值
{
  "products": [
    {
      {
      "id": id, // 商品id
      "name": name, // 商品名称
      "price": price, // 商品价格
      "image_url": image_url, // 商品图片
      "description": description, // 商品描述
      "seller_id": seller_id, // 商品卖家id
      "created_at": created_at, // 商品创建时间
      "updated_at": updated_at, // 商品更新时间
      "category_id": category_id, // 商品分类
      "status": status, // 商品状态  可以是"active","inactive","deleted","sold"之一，分别表示上架，下架，删除，已售出
      },
      ......
    }
  ], // 商品列表
  "message": msg  // msg为服务器返回的信息
}

/api/get_categories

描述：获取所有商品分类
类型：GET

返回值
{
  "categories": [
    {
      "id": id, // 分类id
      "name": name // 分类名称
    },
    ......
  ], // 分类列表
  "message": msg  // msg为服务器返回的信息
}

/api/delete_product/<id>

描述：删除商品id为<id>的商品
类型：DELETE

返回值
{
  "message": msg  // msg为服务器返回的信息
}


/api/buy_product/<id>

描述：购买商品id为<id>的商品
类型：POST

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/get_orders

描述: 获取自己所有订单
类型:GET

返回值
{
  "orders": [
    {
      "id": id, // 订单id
      "product_id": product_id, // 商品id
      "buyer_id": buyer_id, // 购买者id
      "created_at": created_at, // 订单创建时间
      "updated_at": updated_at, // 订单更新时间
      "status": status, // 订单状态  可以是"active","inactive","deleted","sold"之一，分别表示上架，下架，删除，已售出
    },
    ......
  ], // 订单列表
  "message": msg  // msg为服务器返回的信息
}

/api/favorite_product/<id>

描述：收藏商品id为<id>的商品
类型：POST

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/get_favorites

描述：获取自己所有收藏
类型：GET

返回值
{
  "favorites": [
    {
      "product_id": product_id, // 商品id
    },
    ......
  ]
}

/api/delete_favorite/<id>

描述：删除收藏id为<id>的收藏
类型：DELETE

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/publish_comment

描述：发布评论
类型：POST

参数
{
  "product_id": product_id, // 商品id
  "content": content, // 评论内容
}

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/get_comments/<id>

描述：获取商品id为<id>的所有评论
类型：GET

返回值
{
  "comments": [
    {
      "comment_id": comment_id, // 评论id
      "content": content, // 评论内容
      "sender_id": sender_id, // 发送者id
      "rate": rate, // 评分
      "created_at": created_at, // 评论创建时间
    },
    ......
  ]
}

/api/delete_comment/<id>

描述：删除评论id为<id>的评论
类型：DELETE

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/delete_comment/<id>

描述: 删除评论id为<id>的评论
类型：DELETE


返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/send_msg

描述：发送消息
类型：POST

参数
{
  "receiver_id": receiver_id, // 接收者id
  "content": content, // 消息内容
}

返回值
{
  "message": msg  // msg为服务器返回的信息
}

/api/get_msgs

描述：获取自己所有消息
类型：GET

返回值
{
  "messages": [
    {
      "sender_id": sender_id, // 发送者id
      "content": content, // 消息内容
      "created_at": created_at, // 消息创建时间
    },
    ......
  ]
}